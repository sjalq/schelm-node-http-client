"use strict";
const assert=require("node:assert/strict"),{harness,bytes,settle}=require("../support/machine-harness.cjs");
(async()=>{const count=200,chunks=1000,chunk=bytes([1,2,3,4]),before=process.memoryUsage();const hs=[];for(let r=0;r<count;r++)hs.push(harness({limit:chunks*4,chunks:Array.from({length:chunks},()=>chunk)}));await settle();let reads=0,copies=0;for(const h of hs){assert.equal(h.deliveries.length,1);assert.equal(h.deliveries[0].value.body.length,chunks*4);reads+=h.counts().reads;copies+=h.counts().copies;}assert.equal(reads,count*(chunks+1));assert.equal(copies,count*chunks);const after=process.memoryUsage(),growth=(after.heapUsed+after.external+after.arrayBuffers)-(before.heapUsed+before.external+before.arrayBuffers),envelope=count*4*1024*1024;assert.ok(growth<envelope,`growth ${growth} exceeds empirical envelope ${envelope}`);console.log(JSON.stringify({requests:count,chunksPerRequest:chunks,reads,copies,growthBytes:growth,envelopeBytes:envelope}));})().catch(e=>{console.error(e);process.exit(1);});
