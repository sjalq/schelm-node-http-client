"use strict";
const cp=require("node:child_process");const rows=[];for(const encoding of ["gzip","deflate","br"])for(const concurrency of [1,8,32,200]){const out=cp.execFileSync(process.execPath,["--expose-gc","tests/perf/real-memory-child.cjs",encoding,String(concurrency),"65536"],{encoding:"utf8",timeout:180000,maxBuffer:10*1024*1024});const line=out.trim().split("\n").filter(x=>x.startsWith("{" )).at(-1);if(!line)throw Error(`missing memory result ${encoding}/${concurrency}`);rows.push(JSON.parse(line));}console.log(JSON.stringify({kind:"real-compression-memory",cases:rows.length,rows}));
